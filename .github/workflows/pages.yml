name: GitHub Pages (Disabled)

# Deployment is handled by DigitalOcean App Platform.
# This workflow remains as a disabled placeholder to avoid accidental Pages deploys.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Pages deployment disabled
        run: echo "GitHub Pages deployment is disabled. Use DigitalOcean App Platform."
